# Write a Python program to find the area of a triangle.

# input the base and height from the user
base = float(input("Enter the length of the base of the tringle: "))
height = float(input("Enter the height of the ttringle: "))
# calculate the area of the tringle
area = 1.0 * base * height
# display the reslut
print(f"the area of the tringle is: {area}")